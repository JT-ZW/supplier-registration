# Application
APP_NAME=Supplier Registration System
APP_ENV=development
DEBUG=true
API_VERSION=v1

# Server
HOST=0.0.0.0
PORT=8000

# Supabase
# Get these from: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key
# WARNING: Keep service key SECRET! Never expose to frontend!
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# JWT Authentication
# ⚠️ CRITICAL: Generate a strong secret key before production!
# Run: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=CHANGE-THIS-IN-PRODUCTION-USE-64-CHAR-RANDOM-STRING
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# AWS S3 Storage
# Get these from AWS IAM Console
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=supplier-documents
S3_PRESIGNED_URL_EXPIRY=600

# File Upload Security
MAX_FILE_SIZE_MB=20
# Only allow document and image types
ALLOWED_FILE_TYPES=application/pdf,image/jpeg,image/png,image/jpg

# Email (SendGrid) - Recommended for production
# Sign up at: https://sendgrid.com/
SENDGRID_API_KEY=your-sendgrid-api-key
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=Supplier Registration System

# Email (SMTP Alternative) - For development/testing
# For Gmail: Use App Password, not account password
# Generate at: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-16-char-app-password

# Frontend URL
# ⚠️ IMPORTANT: Set to your actual domain in production
# Used for CORS and email links
FRONTEND_URL=http://localhost:3000

# Data Retention Policy
REJECTED_APPLICATION_RETENTION_DAYS=30

# ==============================================
# PRODUCTION DEPLOYMENT CHECKLIST:
# ==============================================
# [ ] Generate strong JWT_SECRET_KEY
# [ ] Set APP_ENV=production
# [ ] Set DEBUG=false
# [ ] Update FRONTEND_URL to production domain
# [ ] Use HTTPS for all URLs
# [ ] Secure all secret keys
# [ ] Enable database RLS policies
# [ ] Configure proper CORS origins
# [ ] Set up monitoring and logging
# [ ] Review rate limiting settings
# ==============================================
